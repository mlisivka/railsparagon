$(".error_msg").remove();
<% if @registration.errors.empty? %>
	$("tbody").append("<%= escape_javascript(render 'team') %>")
<% else %>
	<% @registration.errors.each do |attribute, error| %>
		$("#pick-team").after('<div class="error_msg"><%= error %></div>')
	<% end %>
<% end %>
