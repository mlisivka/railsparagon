$(".error_msg").remove();
<% if @tournament.errors.empty? %>
	$("tbody").append("<%= escape_javascript(render 'team') %>")
<% else %>
	<% @tournament.errors.each do |attribute, error| %>
		$("#pick-team").after('<div class="error_msg"><%= error %></div>')
	<% end %>
<% end %>
